# gemini-web2api 交接日志

## 日期
2026-06-06

## 一、项目概况

### 项目地址
- 本地路径: `d:\workspaces\2api\gemini2api`
- 远程仓库: `https://github.com/Sophomoresty/gemini-web2api.git`
- 当前版本: v1.1.0

### 项目架构
```
gemini2api/
├── gemini_web2api/           # 核心模块包
│   ├── __init__.py          # 版本定义 (v1.1.0)
│   ├── __main__.py          # 入口点: python -m gemini_web2api
│   ├── config.py            # 配置管理
│   ├── gemini.py            # Gemini StreamGenerate 协议实现
│   ├── models.py            # 模型定义与映射
│   ├── multimodal.py        # 图片上传 (Scotty resumable upload)
│   ├── server.py            # HTTP 服务器 (OpenAI 兼容 API)
│   └── tools.py             # 工具调用解析 (Function Calling)
├── gemini_web2api.py         # 单文件版本 (完整功能)
├── config.example.json       # 配置模板
├── requirements.txt          # 依赖: httpx>=0.25 (可选)
├── pyproject.toml            # Python 包配置
├── Dockerfile                # Docker 部署
├── docker-compose.local.yml  # Docker Compose
├── 启动服务器.bat             # Windows 启动脚本
└── start.ps1                 # PowerShell 启动脚本
```

### 核心模块职责
- `config.py`: 配置加载与管理
- `gemini.py`: Gemini StreamGenerate 协议实现，支持 httpx 流式和 urllib 回退
- `server.py`: OpenAI 兼容 HTTP 服务器，支持 `/v1/chat/completions`, `/v1/responses`, `/v1beta/models`
- `tools.py`: Function Calling 工具调用解析
- `multimodal.py`: 图片上传功能

### 可用模型
| 模型 | 说明 |
|------|------|
| `gemini-3.5-flash` | 快速通用模型 |
| `gemini-3.5-flash-thinking` | 深度思考，最长输出 (~2万字) |
| `gemini-3.1-pro` | Pro 模型 (需 cookie) |
| `gemini-auto` | 自动选择模型 |
| `gemini-3.5-flash-thinking-lite` | 自适应思考深度 |
| `gemini-flash-lite` | 轻量快速模型 |

---

## 二、已执行操作

### 1. 版本更新
```bash
git pull origin main
# 结果: 更新了 README.md 和 README_CN.md，核心代码无变化
```

### 2. 依赖安装
```bash
pip install httpx
# 已安装: httpx 0.28.1 + 依赖 (anyio, certifi, httpcore, idna, h11, typing_extensions)
```

### 3. 服务启动
```bash
cd d:\workspaces\2api\gemini2api
python gemini_web2api.py
# 服务运行在: http://localhost:8081
# 进程ID: 75904 (后台运行中)
```

---

## 三、测试结果

### 非流式请求测试 ✅
```
状态码: 200
耗时: 6.02s
模型: gemini-3.5-flash
回复: 你好！很高兴和你交流。今天有什么我可以帮你的吗？
Token用量: {'prompt_tokens': 1, 'completion_tokens': 6, 'total_tokens': 7}
```

### 流式请求测试 ✅
```
状态码: 200
Content-Type: text/event-stream
接收流式数据:
  Chunk 1: 你好！
  Chunk 2: 很高兴和你交流。有什么我可以帮你的？
  [DONE]
总耗时: 7.39s
接收Chunks数: 2
完整回复: 你好！很高兴和你交流。有什么我可以帮你的？
```

### 测试结论
- ✅ 非流式请求正常
- ✅ 流式请求正常 (httpx 真流式)
- ✅ API 兼容性良好

---

## 四、客户端配置信息

### OpenAI 兼容客户端配置
| 配置项 | 值 |
|--------|-----|
| Base URL | `http://localhost:8081/v1` |
| API Key | 随便填或留空 (config.json 中 api_keys 为空时不校验) |
| Model | `gemini-3.5-flash` 或 `gemini-3.5-flash-thinking` |

### curl 测试命令
```bash
# PowerShell
curl -Uri http://localhost:8081/v1/chat/completions -Method POST -ContentType "application/json" -Body '{"model":"gemini-3.5-flash","messages":[{"role":"user","content":"你好"}]}'

# CMD (需要转义引号)
curl.exe -s -X POST http://localhost:8081/v1/chat/completions -H "Content-Type: application/json" -d "{\"model\":\"gemini-3.5-flash\",\"messages\":[{\"role\":\"user\",\"content\":\"你好\"}]}"
```

### Python 测试代码
```python
import urllib.request
import json

data = json.dumps({
    'model': 'gemini-3.5-flash',
    'messages': [{'role': 'user', 'content': '你好'}]
}).encode()
req = urllib.request.Request(
    'http://localhost:8081/v1/chat/completions',
    data=data,
    headers={'Content-Type': 'application/json'}
)
resp = urllib.request.urlopen(req, timeout=60)
print(json.loads(resp.read()))
```

---

## 五、待解决问题

### TRAE 配置问题
用户反馈在 TRAE 设置界面配置后无法使用，需要进一步检查：
1. 用户填写的 Base URL 是否正确
2. 用户填写的 API Key 格式
3. 用户填写的 Model 名称
4. TRAE 的配置文件位置和格式

### TRAE 配置文件位置
- 设置文件: `c:\Users\李昊桐\AppData\Roaming\TRAE SOLO CN\User\settings.json`
- 全局存储: `c:\Users\李昊桐\AppData\Roaming\TRAE SOLO CN\User\globalStorage\storage.json`

---

## 六、环境信息

### 系统环境
- 操作系统: Windows
- Python 版本: 3.12.3
- 工作目录: `d:\workspaces\2api\gemini2api`

### 已安装依赖
- httpx 0.28.1
- anyio 4.13.0
- certifi 2026.5.20
- httpcore 1.0.9
- idna 3.18
- h11 0.16.0
- typing_extensions 4.15.0

---

## 七、下次操作建议

1. **解决 TRAE 配置问题**
   - 获取用户的 TRAE 配置截图
   - 检查配置参数是否正确
   - 测试 TRAE 是否能正常调用 API

2. **配置 config.json** (可选)
   - 如需 API Key 认证，创建 `config.json` 并设置 `api_keys`
   - 如需代理，设置 `proxy` 字段
   - 如需 Cookie (Pro 模型)，设置 `cookie_file` 字段

3. **保持服务运行**
   - 当前服务已在后台运行
   - 如需重启: `python gemini_web2api.py`
   - 如需停止: Ctrl+C 或终止进程

---

## 八、配置模板

### config.json 最小配置
```json
{
  "port": 8081,
  "host": "0.0.0.0",
  "api_keys": [],
  "proxy": null,
  "cookie_file": null,
  "log_requests": true
}
```

### config.json 完整配置
```json
{
  "port": 8081,
  "host": "0.0.0.0",
  "retry_attempts": 3,
  "retry_delay_sec": 2,
  "request_timeout_sec": 180,
  "gemini_bl": "boq_assistant-bard-web-server_20260525.09_p0",
  "auth_user": null,
  "xsrf_token": null,
  "default_model": "gemini-3.5-flash",
  "api_keys": ["sk-your-key"],
  "cookie_file": null,
  "proxy": null,
  "log_requests": true
}
```
